# GameUAV 无人机端系统架构说明

本文档描述 `/home/uav/Desktop/uav_project/gameuav` 仓库中的无人机端运行系统。地面站 GCS 运行在 `20.0.0.172`，这里只作为外部系统展示；本仓库不包含也不应恢复 UAV 本地 `gameuav-gcs.service`。

## 1. 系统总览

```mermaid
flowchart LR
  subgraph Ground["外部地面站 20.0.0.172"]
    GCS["Web GCS\nhttp://20.0.0.172:8000"]
    Operator["操作者 / 浏览器"]
    Operator --> GCS
  end

  subgraph UAV["无人机机载电脑 20.0.0.187"]
    Agent["gameuav-agent\nTCP 8765"]
    NetGW["net_to_ros_gateway\nTCP 9100"]
    CamGW["camera_stream_gateway\nHTTP MJPEG 9200"]
    ROS["ROS master + ROS topics"]

    subgraph Perception["感知"]
      RS["RealSense D435\nrealsense2_camera"]
      RGB1["RGB1 USB camera"]
    end

    subgraph Estimation["状态估计"]
      MAVROS["MAVROS\nPX4 bridge"]
      VINS["VINS-Fusion\n/vins_fusion/imu_propagate"]
    end

    subgraph Planning["规划 / 任务"]
      Ego["EGO-Planner\n/position_cmd"]
      SPF["See-Point-Fly bridge\n可选"]
      SPFWorker["SPF worker\nTCP 9310 可选"]
    end

    subgraph Control["控制 / 执行"]
      PX4Ctrl["px4ctrl\nVINS-only 姿态/推力控制"]
      PX4["PX4 FMU\n串口 FCU"]
      Tiplight["tiplight\n可选"]
    end
  end

  GCS -- "模块控制 / 健康检查" --> Agent
  GCS -- "ROS 命令" --> NetGW
  GCS -- "MJPEG 视频" --> CamGW

  Agent -- "启动/停止白名单模块" --> ROS
  NetGW -- "发布命令 / 采样状态" --> ROS
  ROS -- "图像话题" --> CamGW

  RS -- "infra/depth/color" --> VINS
  RGB1 -- "/rgb1/image_raw" --> CamGW
  MAVROS -- "/mavros/imu/data" --> VINS
  VINS -- "里程计" --> PX4Ctrl
  VINS -- "里程计" --> Ego
  Ego -- "/position_cmd" --> PX4Ctrl
  SPF --> Ego
  SPFWorker --> SPF
  PX4Ctrl -- "/mavros/setpoint_raw/attitude" --> MAVROS
  MAVROS <--> PX4
  Tiplight -. "LED 串口" .-> Control
```

### 总体数据流

- 地面站通过 `gameuav-agent:8765` 管理 UAV 侧模块。
- 地面站通过 `net_to_ros_gateway:9100` 下发 ROS 指令。
- 地面站通过 `camera_stream_gateway:9200` 查看 UAV 侧视频流。
- RealSense 和 PX4 IMU 进入 VINS，输出 `/vins_fusion/imu_propagate`。
- `px4ctrl` 使用 VINS 里程计做闭环控制，输出 `/mavros/setpoint_raw/attitude`。
- MAVROS 将姿态/推力 setpoint 发给 PX4 FMU。

## 2. 启动链路

```mermaid
flowchart TD
  AgentCmd["agentctl / GCS 模块请求"] --> UAVAgent["uav_agent\nconfig/modules/uav_agent.yaml"]

  UAVAgent --> EgoCtrl["egoctrl / realflight\nlaunch/bringup_realflight.launch"]
  UAVAgent --> Standalone["独立模块\nroscore, realsense, vins, px4ctrl, ego, rgb1_camera"]

  EgoCtrl --> FlightControl["bringup_flight_control.launch"]
  EgoCtrl --> EgoLaunch["bringup_ego.launch\nEGO-Planner"]
  EgoCtrl --> OptionalTip["bringup_tiplight.launch\n可选"]
  EgoCtrl --> OptionalSPF["bringup_see_point_fly.launch\n可选"]

  FlightControl --> StateEst["bringup_state_estimation.launch"]
  FlightControl --> Px4CtrlLaunch["bringup_px4ctrl.launch"]

  StateEst --> MavrosLaunch["bringup_mavros.launch"]
  StateEst --> RealsenseLaunch["bringup_realsense.launch"]
  StateEst --> VinsLaunch["bringup_vins.launch"]

  MavrosLaunch --> MAVROS["/mavros"]
  RealsenseLaunch --> RSPrelaunch["realsense_nodelet_prelaunch.py\nnodelet 前硬件 reset"]
  RSPrelaunch --> RSNode["realsense2_camera\n/camera/*"]
  VinsLaunch --> VINS["VINS-Fusion\n/vins_fusion/*"]
  Px4CtrlLaunch --> PX4Ctrl["px4ctrl\n/px4ctrl/*"]
  EgoLaunch --> EgoPlanner["EGO planner + traj server"]
  OptionalSPF --> SPFBridge["see_point_fly_bridge"]
  OptionalTip --> Tiplight["tiplight_serial_node"]

  Standalone --> CamGW["camera_stream_gateway\nTCP 9200"]
  Standalone --> NetGW["net_to_ros_gateway\nTCP 9100"]
  Standalone --> RGB1["bringup_rgb1_camera.launch\n/rgb1/image_raw"]
```

### 常用启动入口

- `egoctrl`: 一键启动 MAVROS、RealSense、VINS、EGO、px4ctrl。
- `flight_control`: 启动状态估计和 px4ctrl，不含 EGO。
- `state_estimation`: MAVROS、RealSense、VINS 组合。
- `camera_stream_gateway`: UAV 本地 MJPEG 视频服务。

## 3. Agent、通信与网关

```mermaid
flowchart LR
  subgraph External["外部客户端"]
    GCS["地面站 GCS\n20.0.0.172:8000"]
    Browser["浏览器"]
    CLI["tools/agentctl.py"]
  end

  subgraph AgentLayer["Agent 层"]
    AgentServer["agent/uav_agent/server.py\nTCP 8765"]
    LaunchMgr["agent/launch_manager\nstart/stop/status"]
    RosCmdExec["agent/ros_command_executor\n安全 ROS 命令"]
    ModuleCfg["config/modules/uav_agent.yaml\n模块白名单"]
    RosCmdCfg["config/ros_commands/ros_command_executor.yaml\n命令白名单"]
  end

  subgraph CommLayer["共享通信层"]
    AgentClient["comm/agent_client.py"]
    AgentProto["comm/protocol/agent_protocol.py"]
    NetProto["comm/protocol/network_protocol.py"]
    JsonSer["comm/serializer/json_serializer.py"]
    TcpLink["comm/tcp_link"]
    UdpLink["comm/udp_link"]
  end

  subgraph Gateways["UAV 网关"]
    NetToRos["gateway/net_to_ros_gateway\nTCP 9100"]
    RosToNet["gateway/ros_to_net_gateway"]
    CamStream["gateway/camera_stream_gateway\nHTTP 9200"]
    Mapping["gateway/topic_mapping\nconfig/topics/ros_interfaces.yaml"]
  end

  subgraph ROS["ROS 运行时"]
    RosMaster["roscore"]
    Topics["ROS topics/services"]
    Modules["MAVROS / VINS / EGO / px4ctrl / cameras"]
  end

  GCS --> AgentClient
  CLI --> AgentClient
  AgentClient --> AgentProto --> AgentServer
  AgentServer --> ModuleCfg
  AgentServer --> LaunchMgr --> Modules
  AgentServer --> RosCmdExec
  RosCmdExec --> RosCmdCfg
  RosCmdExec --> Topics

  GCS --> NetToRos
  NetToRos --> NetProto
  NetToRos --> Mapping
  NetToRos --> Topics
  Topics --> RosToNet --> NetProto

  Browser --> CamStream
  CamStream --> Topics

  RosMaster --- Topics
  Topics --- Modules
  JsonSer --- AgentProto
  TcpLink --- AgentClient
  UdpLink -. "遥测辅助" .- NetProto
```

### 说明

- `config/modules/uav_agent.yaml` 限定 agent 可以启动哪些模块。
- `config/ros_commands/ros_command_executor.yaml` 限定 GCS 能执行哪些 ROS 命令。
- `camera_stream_gateway` 只负责把 ROS 图像话题转成 HTTP MJPEG。
- 当前 UAV 端不运行 GCS Web 后端。

## 4. 感知与状态估计

```mermaid
flowchart TD
  subgraph Sensors["传感器"]
    D435["Intel RealSense D435\nFW 5.17.3.10"]
    USBRGB["RGB1 USB camera\n/dev/video0"]
    PX4IMU["PX4 IMU\nvia MAVLink"]
  end

  subgraph Perception["感知 ROS 节点"]
    Reset["gameuav_usb_camera/realsense_hw_reset\nprelaunch 硬件 reset"]
    RSNode["realsense2_camera\nlaunch/bringup_realsense.launch"]
    RGB1Node["usb_camera_node.py\nbringup_rgb1_camera.launch"]
  end

  subgraph Topics["图像 / IMU 话题"]
    Infra1["/camera/infra1/image_rect_raw"]
    Infra2["/camera/infra2/image_rect_raw"]
    Depth["/camera/depth/image_rect_raw"]
    Color["/camera/color/image_raw"]
    RGB1["/rgb1/image_raw"]
    Imu["/mavros/imu/data"]
  end

  subgraph Estimation["状态估计"]
    WaitInputs["wait_for_vins_inputs.py\n稳定输入门控"]
    VINS["VINS-Fusion\nfast_drone_250.launch"]
    OdomFast["/vins_fusion/imu_propagate\n高速里程计"]
    Odom["/vins_fusion/odometry"]
    Tracks["/vins_fusion/image_track\n/vins_fusion/point_cloud"]
  end

  subgraph Streaming["HTTP 视频流"]
    CamGW["camera_stream_gateway\nMJPEG max 12 FPS"]
    Browser["地面站浏览器"]
  end

  D435 --> Reset --> RSNode
  RSNode --> Infra1
  RSNode --> Infra2
  RSNode --> Depth
  RSNode --> Color
  USBRGB --> RGB1Node --> RGB1
  PX4IMU --> Imu

  Infra1 --> WaitInputs
  Infra2 --> WaitInputs
  Imu --> WaitInputs
  WaitInputs --> VINS
  Infra1 --> VINS
  Infra2 --> VINS
  Imu --> VINS
  VINS --> OdomFast
  VINS --> Odom
  VINS --> Tracks

  Color --> CamGW
  RGB1 --> CamGW
  Depth -. "可选" .-> CamGW
  Infra1 -. "可选" .-> CamGW
  CamGW --> Browser
```

### 当前感知关键点

- RealSense color 默认启用。
- RealSense nodelet 前有预 reset，避免内部 `initial_reset` 引起 udev race。
- MJPEG 网关限制单客户端最新流和帧率，避免快速切换视频导致连接堆积。

## 5. 规划与任务

```mermaid
flowchart LR
  subgraph Inputs["任务输入"]
    GCSGoal["GCS set_goal\n/planning/goal"]
    GCSYaw["GCS set_yaw\n/planning/goal_yaw_deg"]
    SPFCommand["/spf/user_command\n自然语言"]
    RGB1["/rgb1/image_raw"]
    VinsOdom["/vins_fusion/imu_propagate"]
  end

  subgraph SPF["可选 See-Point-Fly"]
    Worker["SPF worker\nstrategy/see_point_fly/worker\nHTTP 9310"]
    Bridge["see_point_fly_bridge\n任务 ROS 桥"]
  end

  subgraph Ego["EGO-Planner 栈"]
    PlannerNode["drone_0_ego_planner_node"]
    PlanEnv["plan_env / grid_map"]
    PathSearch["path_searching / dyn_a_star"]
    BsplineOpt["bspline_opt"]
    TrajServer["drone_0_traj_server"]
  end

  subgraph Outputs["规划输出"]
    PositionCmd["/position_cmd\nquadrotor_msgs/PositionCommand"]
    Trigger["/traj_start_trigger\npx4ctrl 起飞后触发"]
    DebugTraj["/drone_0_planning/*\nbspline / data_display"]
  end

  SPFCommand --> Bridge
  RGB1 --> Bridge
  VinsOdom --> Bridge
  Bridge --> Worker
  Worker --> Bridge
  Bridge --> GCSGoal

  GCSGoal --> PlannerNode
  GCSYaw --> PlannerNode
  VinsOdom --> PlannerNode
  Trigger --> PlannerNode
  PlannerNode --> PlanEnv
  PlannerNode --> PathSearch
  PlannerNode --> BsplineOpt
  BsplineOpt --> TrajServer
  TrajServer --> PositionCmd
  PlannerNode --> DebugTraj
```

### 说明

- EGO-Planner 输出 `/position_cmd`，由 `px4ctrl` 跟踪。
- See-Point-Fly 是可选任务层，默认不随 `egoctrl` 启动。
- `/traj_start_trigger` 由 `px4ctrl` 在起飞后触发，用于允许后续用户命令或规划。

## 6. 控制与 VINS-only 起降

```mermaid
flowchart TD
  subgraph Inputs["控制输入"]
    VINS["/vins_fusion/imu_propagate\nVINS odom"]
    IMU["/mavros/imu/data\nPX4 IMU"]
    State["/mavros/state\nconnected / armed / mode"]
    ExtState["/mavros/extended_state\nlanded_state"]
    RC["/mavros/rc/in\n可选 RC"]
    PositionCmd["/position_cmd\n规划命令"]
    HoverYaw["/px4ctrl/hover_yaw_cmd"]
    TakeoffLand["/px4ctrl/takeoff_land\nTAKEOFF=1 LAND=2"]
  end

  subgraph PX4Ctrl["px4ctrl VINS-only 控制器"]
    FSM["PX4CtrlFSM\nMANUAL_CTRL / AUTO_TAKEOFF / AUTO_HOVER / CMD_CTRL / AUTO_LAND"]
    StartPose["set_start_pose_for_takeoff_land\nstart_pose = VINS pose"]
    TakeoffRef["get_takeoff_land_des\nx/y/yaw 固定, z 爬升"]
    HoverRef["get_hover_des / set_hov_with_odom"]
    CmdRef["get_cmd_des\n规划跟踪"]
    Controller["LinearControl\nKp/Kv 位置速度反馈"]
    Output["Controller_Output_t\n姿态四元数 + 推力"]
  end

  subgraph PX4Path["PX4 指令路径"]
    AttSp["/mavros/setpoint_raw/attitude"]
    MavrosSrv["/mavros/set_mode\n/mavros/cmd/arming"]
    MAVROS["MAVROS"]
    PX4["PX4 FMU"]
  end

  VINS --> FSM
  IMU --> FSM
  State --> FSM
  ExtState --> FSM
  RC --> FSM
  PositionCmd --> FSM
  HoverYaw --> FSM
  TakeoffLand --> FSM

  FSM --> StartPose
  StartPose --> TakeoffRef
  FSM --> HoverRef
  FSM --> CmdRef
  TakeoffRef --> Controller
  HoverRef --> Controller
  CmdRef --> Controller
  VINS --> Controller
  IMU --> Controller
  Controller --> Output --> AttSp --> MAVROS --> PX4
  FSM --> MavrosSrv --> MAVROS

  note1["px4ctrl 已故意不使用 PX4 local pose。\n/mavros/local_position/pose 仍可能由 MAVROS 发布，\n但 px4ctrl 不订阅它。"]
  note1 -.-> FSM
```

### VINS-only 垂直起飞控制逻辑

起飞触发来自：

```text
/px4ctrl/takeoff_land
takeoff_land_cmd = 1
```

`px4ctrl` 进入 `AUTO_TAKEOFF` 后：

```text
start_pose = 当前 VINS pose
des.x = start_x
des.y = start_y
des.z = start_z + takeoff_speed * t
des.yaw = start_yaw
```

控制器使用：

```text
des_acc = des.a + Kv * (des.v - odom.v) + Kp * (des.p - odom.p) + gravity
```

因此起飞期间如果 XY 发生漂移，`des.p - odom.p` 的 XY 误差会产生水平回拉加速度，再转换成 roll/pitch 姿态修正。

当前已调过的 XY 位置增益：

```yaml
Kp0: 2.0
Kp1: 2.0
Kv0: 1.5
Kv1: 1.5
```

## 7. 关键端口与话题

| 类别 | 地址或话题 | 说明 |
|---|---|---|
| UAV agent | `20.0.0.187:8765` | 模块管理 |
| net-to-ROS gateway | `20.0.0.187:9100` | 网络命令到 ROS |
| camera stream gateway | `20.0.0.187:9200` | HTTP MJPEG |
| Ground GCS | `20.0.0.172:8000` | 地面站 Web |
| RealSense IR | `/camera/infra1/image_rect_raw`, `/camera/infra2/image_rect_raw` | VINS 输入 |
| RealSense color | `/camera/color/image_raw` | 视频流/视觉 |
| PX4 IMU | `/mavros/imu/data` | VINS 与控制输入 |
| VINS odom | `/vins_fusion/imu_propagate` | px4ctrl 主定位输入 |
| Planner command | `/position_cmd` | EGO 到 px4ctrl |
| Control output | `/mavros/setpoint_raw/attitude` | px4ctrl 到 PX4 |
| Takeoff/land | `/px4ctrl/takeoff_land` | TAKEOFF=1, LAND=2 |

## 8. 相关图源文件

同目录下还保留了拆分后的 Mermaid 源文件：

- `00_system_overview.mmd`
- `01_launch_bringup.mmd`
- `02_agent_comm_gateway.mmd`
- `03_perception_state_estimation.mmd`
- `04_planning_mission.mmd`
- `05_control_takeoff.mmd`

